apiVersion: v1
kind: Service
metadata:
  name: dr-demo-onboarding-api
  namespace: onboarding
spec:
  type: ClusterIP
  selector:
    app: dr-demo-onboarding-api
  ports:
    - port: 80
      targetPort: 8080
